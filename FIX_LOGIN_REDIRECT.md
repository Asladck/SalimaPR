# ✅ Исправление: Проблема с редиректом после логина

## 🐛 Проблема
После успешного входа в систему пользователь возвращался обратно на страницу логина вместо главной страницы.

## 🔍 Причина
**Несоответствие формата данных между бэкендом и фронтендом:**

### Бэкенд отправлял (snake_case):
```json
{
  "access_token": "eyJhbGc...",
  "refresh_token": "eyJhbGc..."
}
```

### Фронтенд ожидал (camelCase):
```javascript
{
  "accessToken": "eyJhbGc...",
  "refreshToken": "eyJhbGc..."
}
```

**Результат:** Токены не сохранялись в localStorage, поэтому при переходе на главную страницу пользователь считался неавторизованным и редиректился обратно на страницу логина.

## ✅ Решение

Изменены файлы фронтенда для соответствия формату бэкенда.

### Файл: `public/js/api/auth.js`

#### 1. В функции `signIn()`:
**Было:**
```javascript
if (data.accessToken && data.refreshToken) {
    setTokens(data.accessToken, data.refreshToken);
}
```

**Стало:**
```javascript
// Backend returns access_token and refresh_token with underscores
if (data.access_token && data.refresh_token) {
    setTokens(data.access_token, data.refresh_token);
}
```

#### 2. В функции `refreshAccessToken()`:

**Было:**
```javascript
body: JSON.stringify({ refreshToken }),
// ...
if (data.accessToken) {
    setTokens(data.accessToken, data.refreshToken || refreshToken);
}
```

**Стало:**
```javascript
body: JSON.stringify({ refresh_token: refreshToken }),
// ...
// Backend returns access_token with underscore
if (data.access_token) {
    setTokens(data.access_token, data.refresh_token || refreshToken);
}
```

## 🧪 Тестирование

### Проверьте исправление:

1. **Откройте браузер в режиме инкогнито**
   ```
   http://localhost:8080/login.html
   ```

2. **Войдите в систему:**
   - Username: ваш username
   - Password: ваш пароль
   - Нажмите "Sign In"

3. **Ожидаемый результат:**
   ✅ Успешный вход
   ✅ Редирект на главную страницу (index.html)
   ✅ Кнопка "Logout" видна в навигации
   ✅ НЕ редиректит обратно на login

4. **Проверка токенов в localStorage:**
   - Откройте DevTools (F12)
   - Перейдите во вкладку "Application" > "Local Storage"
   - Должны быть два ключа:
     - `accessToken`: значение токена
     - `refreshToken`: значение токена

5. **Проверка защищенных страниц:**
   - Перейдите на `/wardrobe.html`
   - Перейдите на `/outfit.html`
   - ✅ Не должно редиректить на login

6. **Проверка logout:**
   - Нажмите кнопку "Logout"
   - ✅ Должен редиректить на login
   - ✅ Токены должны быть удалены из localStorage

## 📋 Затронутые эндпоинты

### POST /auth/sign-in
**Ответ бэкенда:**
```json
{
  "access_token": "eyJhbGc...",
  "refresh_token": "eyJhbGc..."
}
```

### POST /auth/refresh
**Запрос:**
```json
{
  "refresh_token": "eyJhbGc..."
}
```

**Ответ:**
```json
{
  "access_token": "eyJhbGc..."
}
```

## 🔐 Поток авторизации

```
1. Пользователь вводит логин/пароль
   ↓
2. POST /auth/sign-in
   ↓
3. Бэкенд возвращает access_token и refresh_token
   ↓
4. Фронтенд сохраняет токены в localStorage ✅
   ↓
5. Редирект на главную страницу
   ↓
6. Проверка аутентификации (токен найден) ✅
   ↓
7. Пользователь остается на главной странице ✅
```

## ⚠️ Важно

Если вы в будущем захотите изменить формат ответа бэкенда на camelCase, вам нужно будет:

1. **Изменить в `pkg/handler/auth.go`:**
```go
c.JSON(http.StatusOK, map[string]interface{}{
    "accessToken":  access,  // было: "access_token"
    "refreshToken": refresh, // было: "refresh_token"
})
```

2. **Изменить в `pkg/dto/dto.go`:**
```go
type RefreshInput struct {
    RefreshToken string `json:"refreshToken" binding:"required"` // было: "refresh_token"
}
```

3. **Вернуть изменения во фронтенде:**
   - Изменить обратно на `accessToken` и `refreshToken`

## ✅ Статус

**Проблема:** ✅ ИСПРАВЛЕНА  
**Тестирование:** Готово к тестированию  
**Breaking Changes:** Нет  
**Совместимость:** Фронтенд теперь соответствует бэкенду  

---

**Теперь авторизация работает корректно!** 🎉

После входа пользователь успешно перенаправляется на главную страницу и остается авторизованным.

