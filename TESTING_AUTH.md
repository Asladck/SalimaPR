# 🧪 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ АВТОРИЗАЦИИ

## ✅ Проблема исправлена!

Проблема с редиректом на страницу логина после успешного входа была исправлена.

---

## 🚀 Как протестировать

### Шаг 1: Очистите кэш браузера

**Важно!** Перед тестированием очистите localStorage:

1. Откройте DevTools (F12)
2. Перейдите в "Application" → "Local Storage"
3. Найдите `http://localhost:8080`
4. Удалите все ключи (правый клик → Clear)

**ИЛИ**

Откройте браузер в режиме инкогнито (Ctrl + Shift + N)

---

### Шаг 2: Запустите сервер

```bash
cd C:\Users\DWA\GolandProjects\SalimProject
go run cmd/main.go
```

Убедитесь, что сервер запустился на `http://localhost:8080`

---

### Шаг 3: Зарегистрируйте пользователя (если еще не сделали)

1. Откройте: `http://localhost:8080/signup.html`

2. Заполните форму:
   ```
   Full Name:    Test User
   Username:     testuser
   Email:        test@example.com
   Password:     test123
   ```

3. Нажмите "Create Account"

4. ✅ Должен показать alert "Account created successfully!"

5. ✅ Должен перенаправить на страницу логина

---

### Шаг 4: Войдите в систему

1. Должны быть на странице: `http://localhost:8080/login.html`

2. Введите данные:
   ```
   Username:     testuser
   Password:     test123
   ```

3. Нажмите "Sign In"

4. **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:**
   - ✅ Кнопка показывает спиннер загрузки
   - ✅ Через 1-2 секунды перенаправляет на `http://localhost:8080/index.html`
   - ✅ Видна кнопка "Logout" в навигации
   - ✅ НЕТ редиректа обратно на login

---

### Шаг 5: Проверьте сохранение токенов

1. Откройте DevTools (F12)
2. Вкладка "Application" → "Local Storage" → `http://localhost:8080`
3. **Должны быть два ключа:**
   ```
   accessToken:  eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   refreshToken: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```

✅ Если токены есть - авторизация работает!

---

### Шаг 6: Проверьте защищенные страницы

1. Перейдите на `http://localhost:8080/wardrobe.html`
   - ✅ Должна загрузиться страница "My Closet"
   - ✅ НЕ должен редиректить на login

2. Перейдите на `http://localhost:8080/outfit.html`
   - ✅ Должна загрузиться страница "Outfits"
   - ✅ НЕ должен редиректить на login

3. Перейдите на `http://localhost:8080/index.html`
   - ✅ Должна загрузиться главная страница
   - ✅ Видна кнопка "Logout"

---

### Шаг 7: Проверьте logout

1. Нажмите кнопку "Logout" в навигации
2. Подтвердите в диалоге
3. **ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:**
   - ✅ Перенаправляет на `/login.html`
   - ✅ Токены удалены из localStorage
   - ✅ При попытке зайти на `/index.html` редиректит на `/login.html`

---

## 🔍 Проверка в консоли браузера

Откройте Console в DevTools (F12) и выполните:

```javascript
// Проверить наличие токенов
console.log('Access Token:', localStorage.getItem('accessToken'));
console.log('Refresh Token:', localStorage.getItem('refreshToken'));
```

**Результат после успешного входа:**
```
Access Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjoxLCJleHAiOjE3MzM...
Refresh Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjoxLCJleHAiOjE3MzM...
```

---

## 🐛 Что делать если НЕ работает

### Проблема: Редиректит обратно на login после входа

**Решение:**
1. Откройте DevTools → Console
2. Посмотрите на ошибки (красные сообщения)
3. Проверьте Network → вкладка "Response" для запроса `/auth/sign-in`
4. Убедитесь, что ответ содержит:
   ```json
   {
     "access_token": "...",
     "refresh_token": "..."
   }
   ```

### Проблема: "Failed to fetch"

**Решение:**
1. Убедитесь, что бэкенд запущен
2. Проверьте URL в `public/js/config.js`:
   ```javascript
   baseURL: 'http://localhost:8080'
   ```
3. Проверьте CORS в консоли

### Проблема: "Invalid credentials"

**Решение:**
1. Убедитесь, что пользователь зарегистрирован
2. Проверьте правильность username и password
3. Попробуйте зарегистрировать нового пользователя

---

## ✅ Успешный тест - Чек-лист

- [ ] Регистрация работает
- [ ] После регистрации редиректит на login
- [ ] Вход в систему работает
- [ ] После входа редиректит на главную страницу
- [ ] НЕ редиректит обратно на login
- [ ] Токены сохранены в localStorage
- [ ] Доступ к защищенным страницам работает
- [ ] Кнопка "Logout" видна
- [ ] Logout работает и очищает токены
- [ ] После logout редиректит на login

---

## 📊 Лог успешного входа

В консоли браузера вы должны увидеть (при открытой вкладке Console):

```
Navigated to http://localhost:8080/login.html
[Fetch] POST http://localhost:8080/auth/sign-in (200 OK)
[Response] { "access_token": "...", "refresh_token": "..." }
[localStorage] Set accessToken
[localStorage] Set refreshToken
Navigated to http://localhost:8080/index.html
```

---

## 🎉 Готово!

Если все шаги выполнены успешно, авторизация работает корректно!

**Вы можете:**
- ✅ Регистрироваться
- ✅ Входить в систему
- ✅ Оставаться авторизованным
- ✅ Работать со всеми страницами
- ✅ Выходить из системы

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте консоль браузера на ошибки
2. Проверьте логи бэкенда
3. Убедитесь, что PostgreSQL запущен
4. Очистите localStorage и попробуйте снова

**Удачного тестирования!** 🚀

